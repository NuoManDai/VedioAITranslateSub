# FastAPI Backend Dependencies
# Note: Base project dependencies should be installed separately via root requirements.txt

# FastAPI and related
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# For async file operations
aiofiles>=23.2.1

# For API validation and HTTP client
httpx>=0.25.0

# For video info detection (duration, etc.)
opencv-python>=4.8.0

# For audio processing (used by demucs workaround)
soundfile>=0.12.0
pydub>=0.25.1
